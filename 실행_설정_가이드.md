# TFmini Machine Flow 실행 및 설정 가이드

이 문서는 `tfmini_machine_flow/`만 대상으로 합니다. 기존 루트의 `main.py`, `config.py`, `hardware/` 파일은 수정하지 않습니다.

## 1. 실행 방법

터미널에서 작업 폴더가 `/home/pi/Desktop`인지 확인한 뒤 실행합니다.

```bash
cd /home/pi/Desktop
python3 tfmini_machine_flow/main.py
```

종료는 `Ctrl+C`를 누르면 됩니다. 종료 시 웜기어 모터, 리니어 액추에이터, 서보, 센서 close 처리를 수행합니다.

## 2. 전체 동작 순서

1. TFmini 거리센서를 UART로 연결합니다.
2. 리니어 액추에이터를 `ACTUATOR_TIME` 동안 전진시킵니다.
3. 웜기어 모터를 계속 동작시킵니다.
4. 거리값이 `TRIGGER_DIST` 이하로 `DETECT_COUNT`번 연속 들어오면 물체 감지로 판단합니다.
5. 웜기어 모터를 정지합니다.
6. 스텝모터를 +90도 회전합니다.
7. 서보모터를 +방향으로 2초 동안 약 4도 이동합니다.
8. 웜기어 모터를 5초 동안 동작시킨 뒤 정지합니다.
9. 서보모터를 -방향으로 2초 동안 약 4도 복귀합니다.
10. 웜기어 모터를 다시 동작시킵니다.
11. 거리값이 `RESET_DIST` 이상 멀어져야 다음 감지가 가능합니다.

## 3. 내가 수정할 값 위치

수정할 값은 모두 아래 파일에 있습니다.

```text
tfmini_machine_flow/config.py
```

## 4. 거리센서 관련 설정

```python
TFMINI_PORT = "/dev/serial0"
TFMINI_BAUDRATE = 115200
TRIGGER_DIST = 60
RESET_DIST = 62
DETECT_COUNT = 3
```

- `TFMINI_PORT`: TFmini가 연결된 UART 포트입니다. 기존 설정은 `/dev/serial0`입니다.
- `TFMINI_BAUDRATE`: TFmini 통신 속도입니다. 기존 설정은 `115200`입니다.
- `TRIGGER_DIST`: 이 거리 이하이면 감지 후보가 됩니다. 현재 `60cm`입니다.
- `RESET_DIST`: Flow 실행 후 이 거리 이상 멀어져야 다시 감지됩니다. 현재 `62cm`입니다.
- `DETECT_COUNT`: 연속으로 몇 번 감지되어야 Flow를 실행할지 정합니다. 현재 `3`입니다.

주의: 같은 물체가 60cm 이하에 계속 있으면 Flow가 반복 실행되지 않습니다.

## 5. 리니어 액추에이터 설정

```python
ACTUATOR_SPEED = 0.6
ACTUATOR_TIME = 1.0
ACTUATOR_PINS = {
    "rpwm": 18,
    "lpwm": 19,
    "l_en": 23,
    "r_en": 24,
}
```

- `ACTUATOR_SPEED`: 액추에이터 전진 PWM 속도입니다.
- `ACTUATOR_TIME`: 프로그램 시작 시 액추에이터를 전진시키는 시간입니다.
- `ACTUATOR_PINS`: 액추에이터 드라이버 GPIO 핀입니다.

TODO: 실제로 3cm 전진하는 데 걸리는 시간을 측정해서 `ACTUATOR_TIME`을 보정해야 합니다.

## 6. 웜기어 모터 설정

```python
WORM_SPEED = 0.30
WORM_RUN_SECONDS = 5.0
WORM_MOTOR_PINS = [
    {"rpwm": 12, "lpwm": 13, "l_en": 5, "r_en": 6},
    {"rpwm": 20, "lpwm": 21, "l_en": 16, "r_en": 26},
]
```

- `WORM_SPEED`: 웜기어 모터 PWM 속도입니다. 기존 설정값을 그대로 반영했습니다.
- `WORM_RUN_SECONDS`: Flow 중간에서 웜기어 모터가 동작하는 시간입니다. 현재 `5초`입니다.
- `WORM_MOTOR_PINS`: 웜기어 모터 드라이버 GPIO 핀 목록입니다.

주의: `WORM_SPEED`는 임의로 크게 올리지 말고 실제 부하 상태에서 조금씩 확인하세요.

## 7. 스텝모터 설정

```python
STEPS_90 = 1600
STEP_DELAY = 0.001
DIR_DELAY = 0.02
STEPPER_PINS = {
    "pul": 17,
    "dir": 27,
}
```

- `STEPS_90`: +90도 회전에 필요한 펄스 수입니다.
- `STEP_DELAY`: 펄스 사이 지연 시간입니다.
- `DIR_DELAY`: 방향 설정 후 기다리는 시간입니다.
- `STEPPER_PINS`: PUL/DIR 핀 번호입니다.

TODO: 드라이버 마이크로스텝 설정이 바뀌면 `STEPS_90`을 다시 맞춰야 합니다.

## 8. 서보모터 설정

```python
SERVO_GPIO = 22
SERVO_SPEED = 2.0
SERVO_DEG_PER_SEC = 2.0
SERVO_MOVE_SECONDS = 2.0
SERVO_MOVE_DEG = SERVO_DEG_PER_SEC * SERVO_MOVE_SECONDS
SERVO_HOME_ANGLE = 0.0
SERVO_MIN_ANGLE = -90.0
SERVO_MAX_ANGLE = 90.0
SERVO_MIN_PULSE_WIDTH = 0.5 / 1000
SERVO_MAX_PULSE_WIDTH = 2.5 / 1000
```

- `SERVO_GPIO`: 서보 PWM GPIO 핀입니다.
- `SERVO_DEG_PER_SEC`: 서보 이동 속도입니다. 현재 초당 `2도`입니다.
- `SERVO_MOVE_SECONDS`: 한 방향 이동 시간입니다. 현재 `2초`입니다.
- `SERVO_MOVE_DEG`: 실제 이동 각도입니다. 현재 `4도`입니다.
- `SERVO_HOME_ANGLE`: 시작 기준 각도입니다.
- `SERVO_MIN_ANGLE`, `SERVO_MAX_ANGLE`: 서보가 넘지 않아야 하는 안전 각도 범위입니다.
- `SERVO_MIN_PULSE_WIDTH`, `SERVO_MAX_PULSE_WIDTH`: 서보 PWM 펄스폭입니다.

TODO: 실제 서보와 기구물에 맞게 안전 각도 범위와 펄스폭을 확인하세요.

## 9. 수동 테스트 방법

부품별로 먼저 테스트한 뒤 전체 실행을 권장합니다.

```bash
python3 tfmini_machine_flow/tests_manual/test_distance_sensor.py
python3 tfmini_machine_flow/tests_manual/test_actuator.py
python3 tfmini_machine_flow/tests_manual/test_worm_motor.py
python3 tfmini_machine_flow/tests_manual/test_stepper.py
python3 tfmini_machine_flow/tests_manual/test_servo.py
```

각 테스트는 `Ctrl+C`로 중단할 수 있습니다.

## 10. 수정하면 안 되는 파일

이번 별도 프로젝트에서는 아래 기존 파일/폴더를 수정하지 않습니다.

```text
main.py
config.py
hardware/
sequences/
flows/
tests_manual/
archive/
```

새 Flow 관련 수정은 `tfmini_machine_flow/` 아래에서만 진행하세요.
